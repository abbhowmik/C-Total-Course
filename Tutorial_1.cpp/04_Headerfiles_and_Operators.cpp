#include <iostream> // it is system headerfile(cout, cin's are present in the headerfiles)
// #include "this.h" // this is called user defined headerfiles, if this file is not present in your system, this will throws an error
using namespace std;

int main()
{
    int a = 8, b = 5;
    // cout << "Hello world :" << endl;
    // cout << "Following are the types of operators in c++" << endl;
    // // arithmatic operators
    // cout << "The value of a+b is : " << a + b << endl;
    // cout << "The value of a-b is : " << a - b << endl;
    // cout << "The value of a*b is : " << a * b << endl;
    // cout << "The value of a/b is : " << a / b << endl;
    // cout << "The value of a%b is : " << a % b << endl;

    // cout << "The value of a++ is : " << a++ << endl;
    // cout << "The value of a is : " << a << endl;
    // cout << "The value of ++a is : " << ++a << endl;
    // cout << "The value of a-- is : " << a-- << endl;
    // cout << "The value of a is : " << a << endl;
    // cout << endl;

    // Assignment Operators, this is used to assign to variables
    // int a = 4, b = 5;
    // char d = 's';

    // // Comparison Opeartors
    // cout << "The value of a==b is: " << (a == b) << endl;
    // cout << "The value of a!=b is: " << (a != b) << endl;
    // cout << "The value of a<=b is: " << (a <= b) << endl;
    // cout << "The value of a>=b is: " << (a >= b) << endl;
    // cout << "The value of a>b is: " << (a > b) << endl;

    return 0;
}
